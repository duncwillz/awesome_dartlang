// Built-in types in dart

// Boolean

var young = true; //Inferred to be boolean

String name = "";
var first = 2;
var second = 6;

// Using boolean operator

bool status = first <= second;
var similar = first == second;

void main(){
print(status);
print(similar);
print(name.isEmpty);
}
